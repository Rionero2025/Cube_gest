import { NextResponse } from "next/server";import bcrypt from "bcryptjs";import { getSupabaseAdmin } from "@/lib/supabaseServer";export const dynamic="force-dynamic";export const runtime="nodejs";const plans=[{name:"Free",price_monthly:0,max_users:1,max_clients:3,max_contracts:3,features:["Fino a 3 contratti","1 utente admin"]},{name:"Starter",price_monthly:9,max_users:2,max_clients:10,max_contracts:10,features:["10 clienti/contratti","1 staff aggiuntivo"]},{name:"Professional",price_monthly:29,max_users:4,max_clients:30,max_contracts:30,features:["30 clienti","3 staff aggiuntivi"]},{name:"Business",price_monthly:49,max_users:11,max_clients:100,max_contracts:100,features:["100 clienti","10 staff aggiuntivi"]},{name:"Enterprise",price_monthly:99,max_users:999999,max_clients:999999,max_contracts:999999,features:["Tutto illimitato","Supporto prioritario"]}];export async function GET(){try{const s=getSupabaseAdmin();for(const p of plans){const {error}=await s.from("plans").upsert(p,{onConflict:"name"});if(error)throw error}const password_hash=await bcrypt.hash("admin123",10);const {error}=await s.from("app_users").upsert({username:"superadmin",email:"admin@cube.local",password_hash,nome:"Super",cognome:"Admin",ruolo:"SUPER_ADMIN",status:"ATTIVO"},{onConflict:"username"});if(error)throw error;return NextResponse.json({ok:true,message:"Setup completato. Super Admin: superadmin / admin123"})}catch(error){return NextResponse.json({ok:false,error:String(error),hint:"Esegui prima supabase/schema.sql e controlla NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY su Vercel."},{status:500})}}