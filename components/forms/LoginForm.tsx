"use client";import { useState } from "react";import Link from "next/link";export function LoginForm(){const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMsg("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});const d=await r.json();setLoading(false);if(!d.ok){setMsg(d.error||"Errore login");return}window.location.href=d.redirectTo||"/dashboard"}return <form className="form-box" onSubmit={submit}><div className="form-grid"><input className="input full-row" placeholder="Username o email" value={username} onChange={e=>setUsername(e.target.value)}/><input className="input full-row" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{msg&&<div className="alert full-row">{msg}</div>}<button className="btn btn-primary full-row" disabled={loading}>{loading?"Accesso...":"Accedi"}</button><p className="full-row">Non hai account? <Link href="/registrati">Registrati</Link></p></div></form>}