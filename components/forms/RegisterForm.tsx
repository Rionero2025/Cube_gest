"use client";import { useSearchParams } from "next/navigation";import { useState } from "react";export function RegisterForm(){const sp=useSearchParams();const[f,setF]=useState({ragioneSociale:"",partitaIva:"",codiceFiscale:"",telefono:"",nome:"",cognome:"",email:"",password:"",planName:sp.get("plan")||"Free"});const[msg,setMsg]=useState("");const[loading,setLoading]=useState(false);function u(n:string,v:string){setF(p=>({...p,[n]:v}))}async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMsg("");const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const d=await r.json();setLoading(false);if(!d.ok){setMsg(d.error||"Errore registrazione");return}window.location.href=d.redirectTo||"/dashboard"}return <form className="form-box" onSubmit={submit}><div className="form-grid"><input className="input full-row" placeholder="Ragione sociale" value={f.ragioneSociale} onChange={e=>u("ragioneSociale",e.target.value)}/><input className="input" placeholder="Partita IVA" value={f.partitaIva} onChange={e=>u("partitaIva",e.target.value)}/><input className="input" placeholder="Codice fiscale" value={f.codiceFiscale} onChange={e=>u("codiceFiscale",e.target.value)}/><input className="input" placeholder="Nome admin" value={f.nome} onChange={e=>u("nome",e.target.value)}/><input className="input" placeholder="Cognome admin" value={f.cognome} onChange={e=>u("cognome",e.target.value)}/><input className="input" placeholder="Telefono" value={f.telefono} onChange={e=>u("telefono",e.target.value)}/><select className="input" value={f.planName} onChange={e=>u("planName",e.target.value)}><option>Free</option><option>Starter</option><option>Professional</option><option>Business</option><option>Enterprise</option></select><input className="input full-row" type="email" placeholder="Email admin" value={f.email} onChange={e=>u("email",e.target.value)}/><input className="input full-row" type="password" placeholder="Password" value={f.password} onChange={e=>u("password",e.target.value)}/>{msg&&<div className="alert full-row">{msg}</div>}<button className="btn btn-primary full-row" disabled={loading}>{loading?"Creazione...":"Crea azienda e inizia"}</button></div></form>}